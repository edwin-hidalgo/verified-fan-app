import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['127.0.0.1', 'spongy-evil-wrinkly.ngrok-free.dev', 'verified-fan-app.vercel.app'],
};

export default nextConfig;
